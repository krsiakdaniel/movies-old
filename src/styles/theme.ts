/* eslint-disable prefer-destructuring,no-magic-numbers */

// layout
const breakpoints: any = [480, 576, 768, 992, 1200, 1600];

// colors
const colors = {
  black: '#000000',
  white: '#ffffff',
};

export const theme = {
  breakpoints,
  colors,
};
